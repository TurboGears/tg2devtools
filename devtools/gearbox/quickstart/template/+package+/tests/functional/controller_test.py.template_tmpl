{{start_braces}}# gearbox: output-stem=test_{target} {{end_braces}}
# -*- coding: utf-8 -*-
"""Functional tests for the {{start_braces}}target.capitalize(){{end_braces}} controller."""

from {{package}}.tests import TestController


class Test{{start_braces}}target.capitalize(){{end_braces}}Controller(TestController):
    """Tests for the mounted {{start_braces}}target.lower(){{end_braces}} controller."""

    def test_index(self):
        response = self.app.get('/{{start_braces}}target.lower(){{end_braces}}')

        assert response.status_int == 200
